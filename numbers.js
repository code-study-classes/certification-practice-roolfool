function calculateDistance(x1, x2) {
  const result = Math.abs(x1 - x2);
  return result;
}

console.log(calculateDistance(0, 5));

function calculateSegmentProduct() {
}
export {
  calculateDistance, calculateSegmentProduct,
};
